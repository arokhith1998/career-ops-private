# Product Manager, Platform — Benchling

**URL:** https://zapply.jobs/l/d/ashby-benchling-358ff26c-7b23-4645-b906-2244c76e2cb2?s=gh-new-grad-jobs-2027
**Location:** San Francisco, CA (hybrid — in office Mon/Tue/Thu per Benchling's standard hybrid model)
**Track:** us | **Extracted:** 2026-09-19
**pack_incomplete note:** `output/Benchling/pmm-platform/` already holds a partial pack (resume HTML/PDF + cover letter HTML/PDF, no deck, no outreach.md) for a DIFFERENT Benchling role — "Product Marketing Manager, Platform" (confirmed via the resume's own `<title>` and tag line), not this "Product Manager, Platform" (PM function, not PMM) req. No tracker row exists for either. This is a separate, currently-live posting; the old PMM-Platform pack is a prior run's leftover with no corresponding row in data/applications.md — flagged in the digest for the candidate/build run, not touched here (discover writes no artifacts).

**Extraction method:** WebFetch to `zapply.jobs` and `jobs.ashbyhq.com` was blocked by this session's cloud network egress policy (EGRESS_BLOCKED). Reconstructed from WebSearch (Ashby posting indexed directly, plus freehire.me mirror). The exact UUID (358ff26c-...) could not be confirmed against the indexed "Product Manager, Platform" listing byte-for-byte, but title, company and role family all match; treat as a strong match, not a verbatim fetch.

## Description (paraphrased from indexed copy)
Benchling is building the AI platform for biotech R&D — scientists design experiments, capture structured data, and run AI agents/models in their workflows. The Platform PM family at Benchling owns schema/data-model or developer-platform layers that other product teams, integrations, and AI features build on.

## Work authorization
Application flow asks "whether you will now or in the future require visa sponsorship" (a standard EEO-style intake question, not itself a wall or an offer). No stated "no sponsorship" wall found. Benchling, Inc. has real H-1B filing history (see data/visa-cache.tsv).

## Comp
$163K-$307K reported for the Benchling Platform PM family (source: indexed Ashby posting copy). Comfortably above the candidate's target_range.

## Experience
No explicit years-of-experience floor found in indexed snippets.
